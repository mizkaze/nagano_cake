<h1>Public::CartItems#index</h1>

<h3>ショッピングカート</h3>

<% @cart_items.each do |cart_item| %>

  <table>
    <thead>
      <tr>
        <th>商品名</th>
        <th>単価(税込)</th>
        <th>数量</th>
        <th>小計</th>
        <th></th>
      </tr>
    </thead>

    <tbody>
      <tr>
        <th><%= cart_item.item.name %></th>
        <th><%= cart_item.item.add_tax_price %></th>
        <th><%= cart_item.amount %></th>
        <th><%= cart_item.item.add_tax_price * cart_item.amount %></th>
        <th></th>
      </tr>
    </tbody>
  </table>

<% end %>

