<h1>Public::Orders#show</h1>

<h3>注文履歴詳細</h3>

<strong>注文情報</strong>


<table>
  <tr>
    <th>注文日</th>
    <td><%= l @order.created_at%></td>
  </tr>
  <tr>
    <th>配送先</th>
    <td>
      <%= @order.postal_code %><br>
      <%= @order.address %><br>
      <%= @order.name %>
    </td>
  </tr>
  <tr>
    <th>支払い方法</th>
    <td><%= @order.method_of_payment_i18n %></td>
  </tr>
  <tr>
    <th>ステータス</th>
    <td><%= @order.status_i18n %></td>
  </tr>
</table>

<strong>請求情報</strong>
<table>
  <tr>
    <th>商品合計</th>
    <td><%= @total.to_s(:delimited) %></td>
  </tr>
  <tr>
    <th>配送料</th>
    <td><%= @postage.to_s(:delimited) %></td>
  </tr>
  <tr>
    <th><strong>ご請求額</strong></th>
    <td><%= @billing_amount.to_s(:delimited) %></td>
  </tr>
</table>

<strong>注文内容</strong>
<table>
  <thead>
    <tr>
      <th>商品</th>
      <th>単価(税込)</th>
      <th>個数</th>
      <th>小計</th>
    </tr>
  </thead>

  <tbody>
    <% @order_details.each do |detail| %>
      <tr>
        <td><%= detail.item.name %></td>
        <!--テーブル名(ここではitem)はアソシエーションによって複数形か単数形か書き換える-->
        <td><%= detail.price_with_tax.to_s(:delimited) %></td>
        <td><%= detail.amount %></td>
        <td><%= detail.sum_of_price.to_s(:delimited) %></td>
      </tr>
    <% end %>
  </tbody>
</table>