<h1>Admin::Genres#edit</h1>

<h3>ジャンル編集</h3>
<!--パス分からない-->
<%= form_with model: @genre, url: admin_genre_path(@genre.id), method: :patch do |f| %>

  <label>ジャンル名</label>
  <%= f.text_field :name %>
  <%= f.submit '変更を保存' %>

<% end %>